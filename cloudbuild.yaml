steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/nursery-app:$COMMIT_SHA', '-t', 'gcr.io/$PROJECT_ID/nursery-app:latest', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/nursery-app:$COMMIT_SHA']
  
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/nursery-app:latest']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'nursery-app'
      - '--image=gcr.io/$PROJECT_ID/nursery-app:$COMMIT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--set-env-vars=FLASK_ENV=production,DB_HOST=34.10.117.44,DB_USER=root,DB_NAME=nursery_db,DB_PORT=3306'
      - '--set-secrets=DB_PASSWORD=db-password:latest,SECRET_KEY=flask-secret-key:latest'

images:
  - 'gcr.io/$PROJECT_ID/nursery-app:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/nursery-app:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'N1_HIGHCPU_8'
